# Database connection
db_dsn=postgres://postgres:password@localhost:5432/observability?sslmode=disable

# GitHub configuration
github_token=ghp_exampletoken
github_repo=owner/repo

# Optional ArgoCD health check
argocd_url=http://localhost:8081
argocd_app=my-app

# OpenObserve credentials for docker-compose
openobserve_email=root@example.com
openobserve_password=changeme
